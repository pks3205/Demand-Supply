 फ़ाइलें
BUILD_APK.md (अभी viewer में खुली है) — पूरी instructions
.github/workflows/android-build.yml — यही पूरा workflow है (workspace में मौजूद)
मैंने मेंशन किया PR #1 — वह आपके main branch पर merge करने के लिए तैयार है। 💪

क्या आप चाहते हैं कि मैं release APK (signed) भी workflow में add करूँ, या फिलहाल debug APK ही काफी है?


Please merge the pull request.


 sonnet vs llama   >> llama wins!               
No file chosen




Demand-Supply
arena/01a05c8b-demand-supply

+5944
-1
Merge PR
Workspace

Diff
Working…

Branch
unified
split


.github/workflows/android-build.yml
+54
name: Build Android APK

on:
  push:
    branches:
      - main
      - arena/01a05c8b-demand-supply
  workflow_dispatch:

concurrency:
  group: android-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-apk:
    name: Build debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install npm dependencies
        run: npm ci

      - name: Build the web app
        run: npm run build

      - name: Sync web assets into Capacitor Android project
        run: npx cap sync android

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      - name: Build debug APK
        working-directory: android
        run: ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: sabzi-mandi-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
